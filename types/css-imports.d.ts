// Side-effect CSS imports (e.g. `import 'katex/dist/katex.min.css'`) — Next's
// next-env.d.ts used to pull this in transitively via its own global type
// references; now that Next is gone, declare it directly instead.
declare module '*.css'
