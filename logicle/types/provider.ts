export enum ProviderType {
  OpenAI = 'openai',
  Anthropic = 'anthropic',
  LogicleCloud = 'logiclecloud',
  GcpVertex = 'gcp-vertex',
}

export enum ModelDetectionMode {
  AUTO = 'Auto',
  MANUAL = 'Manual',
}
